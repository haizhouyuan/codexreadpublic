# mem0 + Qdrant (optional long-term memory, shared across projects)
MEM0_ENABLED=false

# Absolute path recommended (avoid relative-path confusion across projects)
MEM0_CONFIG_PATH=/vol1/1000/projects/codexread/examples/mem0/mem0_config.yaml

# OpenAI-compatible base url + key used by mem0 (LLM + embedder)
MEM0_LLM_BASE_URL=http://127.0.0.1:8000/v1
MEM0_LLM_API_KEY=your_mem0_llm_api_key

# Optional: namespace memories across agents via metadata
MEM0_AGENT_ID=codexread

# Recommended: mark which project wrote the memory (helps audit in a shared backend)
MEM0_ORIGIN_PROJECT=codexread

# Subject user_ids
# - Recommended for multi-project sharing: make them stable "person IDs" (not device IDs), and keep them the SAME across repos.
# - If you prefer strict isolation per repo, you can still use repo-based values.
U1_USER_ID=family_u1
CHILD_USER_ID=family_child
